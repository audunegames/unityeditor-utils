namespace Audune.Utils
{
  // Enum that defines options how a reorderable list will be drawn
  public enum ReorderableListDrawOptions
  {
    None = 0,
    DrawFoldout = 1 << 0,
    DrawInfoField = 1 << 1,
  }
}